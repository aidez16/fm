# Single-node Kafka (KRaft mode) for demoing the streaming path.
#
# Demo only: one replica, no persistent volume, no auth. For real use, swap in
# a managed Kafka or Strimzi and repoint KAFKA_BOOTSTRAP_SERVERS.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kafka
  namespace: future-movement-summary
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kafka
  template:
    metadata:
      labels:
        app: kafka
    spec:
      containers:
        - name: kafka
          image: apache/kafka:3.8.0
          ports:
            - containerPort: 9092
          env:
            - name: KAFKA_NODE_ID
              value: "1"
            - name: KAFKA_PROCESS_ROLES
              value: broker,controller
            - name: KAFKA_LISTENERS
              value: PLAINTEXT://0.0.0.0:9092,CONTROLLER://0.0.0.0:9093
            - name: KAFKA_ADVERTISED_LISTENERS
              value: PLAINTEXT://kafka.future-movement-summary.svc.cluster.local:9092
            - name: KAFKA_LISTENER_SECURITY_PROTOCOL_MAP
              value: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT
            - name: KAFKA_CONTROLLER_LISTENER_NAMES
              value: CONTROLLER
            - name: KAFKA_CONTROLLER_QUORUM_VOTERS
              value: 1@localhost:9093
            - name: KAFKA_INTER_BROKER_LISTENER_NAME
              value: PLAINTEXT
            - name: KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR
              value: "1"
            - name: KAFKA_AUTO_CREATE_TOPICS_ENABLE
              value: "true"
            - name: CLUSTER_ID
              value: "ZmZmZmZmZmZmZmZmZmZmZm"
---
apiVersion: v1
kind: Service
metadata:
  name: kafka
  namespace: future-movement-summary
spec:
  selector:
    app: kafka
  ports:
    - port: 9092
      targetPort: 9092
