export const environment = {
  production: true,
  // Same-origin: nginx routes /api to the backend (see nginx.conf). Point at
  // an absolute URL if the API is hosted elsewhere.
  apiBaseUrl: '/api/v1/future-movements',
};
